---
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    install:
      - python -m pip install --upgrade pip
      - python -m pip install --group docs

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py
